{
  "name": "@gin/workflows",
  "version": "0.1.0",
  "description": "Gin workflow DSL: declarative tool/model/approval pipelines compiled onto the durable engine.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@gin/core": "workspace:*",
    "@gin/cost": "workspace:*",
    "@gin/durable": "workspace:*",
    "@gin/governance": "workspace:*",
    "@gin/models": "workspace:*",
    "@gin/tools": "workspace:*",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@gin/storage": "workspace:*",
    "@types/node": "^24.0.0",
    "tsup": "^8.4.0",
    "typescript": "^5.8.0",
    "vitest": "^4.1.0"
  }
}
