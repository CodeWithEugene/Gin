{
  "name": "@gin/observability",
  "version": "0.1.0",
  "description": "Gin observability: persistent trace store fed by the event bus — step-level glass-box tracing.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@gin/core": "workspace:*",
    "@gin/storage": "workspace:*"
  },
  "devDependencies": {
    "@gin/memory": "workspace:*",
    "@gin/models": "workspace:*",
    "@gin/runtime": "workspace:*",
    "@gin/tools": "workspace:*",
    "@types/node": "^24.0.0",
    "tsup": "^8.4.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
